// 在应用程序没有键盘焦点时，监听键盘事件

import React from "react";

import styles from "./index.module.scss";

const GlobalShortcut = () => {
  return (
    <div className={styles.content}>
      <div>GlobalShortcut</div>
    </div>
  );
};

export default GlobalShortcut;
